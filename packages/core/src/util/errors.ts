/**
 * Marker for scaffolded stubs awaiting implementation (Phase 0 tasks 0.2-0.5).
 * Implementers replace call sites with real bodies; the string names the missing unit.
 */
export function notImplemented(what: string): never {
  throw new Error(`NOT_IMPLEMENTED: ${what}`);
}
